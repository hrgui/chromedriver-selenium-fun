Harman's selenium chromedriver fun
===

# prereqs
1. npm install selenium-standalone && selenium-standalone install

## Steps to run
1. selenium-standlone start (on one tab)
2. node chromedriver-test.js